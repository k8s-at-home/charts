image:
  repository: b4bz/homer
  tag: 20.09.1

strategy:
  type: Recreate

service:
  port:
    port: 8080

ingress:
  enabled: true

persistence:
  config:
    enabled: false
    emptyDir: false
    mountPath: /www/assets
