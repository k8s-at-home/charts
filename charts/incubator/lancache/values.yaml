#
# IMPORTANT NOTE
#
# This chart inherits from our common library chart. You can check the default values/options here:
# https://github.com/k8s-at-home/library-charts/tree/main/charts/stable/common/values.yaml
#

image:
  # -- image repository
  repository: lancachenet/monolithic
  # -- image tag
  tag: latest
  # -- image pull policy
  pullPolicy: Always

# -- environment variables. See more environment variables in the [lancache documentation](https://lancache.org/docs).
# @default -- See below
env: {}
  # UPSTREAM_DNS:
  # CACHE_MEM_SIZE:
  # CACHE_DISK_SIZE:
  # CACHE_MAX_AGE:

# -- Configures service settings for the chart.
# @default -- See values.yaml
service:
  main:
    # type: LoadBalancer
    # loadBalancerIP: 192.168.1.80
    ports:
      http:
        port: 80
      https:
        enabled: true
        port: 443

dns:
  # -- Enable and configure LanCache DNS sidecar.
  # @default -- See values.yaml
  enabled: false
  env: {}
  image:
    # -- image repository
    repository: lancachenet/lancache-dns
    # -- image tag
    tag: latest
    # -- image pull policy
    pullPolicy: Always

# -- Configure persistence settings for the chart under this key.
# @default -- See values.yaml
persistence:
  data:
    enabled: false
    mountPath: /data/cache
  logs:
    enabled: false
    mountPath: /data/logs

# -- The official LanCache image is only available for x86_64.
nodeSelector:
  kubernetes.io/arch: amd64
