{{/* Make sure all variables are set properly */}}
{{- include "common.values.setup" . }}

{{/* Append the hardcoded settings */}}
{{- define "openbooks.hardcodedValues" -}}
args:
  {{ with .Values.persistence.books.enabled }}
    - "--persist"
  {{ end }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "openbooks.hardcodedValues" . | fromYaml) -}}

{{/* Render the templates */}}
{{ include "common.all" . }}