apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "common.names.fullname" . }}-whitelist
  labels:
    {{- include "common.labels" . | nindent 4 }}
data:
  whitelist.txt: |
{{ .Values.bridges | indent 4 }}
