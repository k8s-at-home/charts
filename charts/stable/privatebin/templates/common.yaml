{{/* Make sure all variables are set properly */}}
{{- include "common.values.setup" . }}

{{/* Append the configMap volume to the volumes */}}
{{- define "privatebin.configVolume" -}}
enabled: "true"
mountPath: "/srv/cfg/conf.php"
subPath: "conf.php"
type: "custom"
volumeSpec:
  configMap:
    name: {{ template "common.names.fullname" . }}-config
{{- end -}}
{{- $_ := set .Values.persistence "privatebin-config" (include "privatebin.configVolume" . | fromYaml) -}}

{{ include "common.all" . }}
