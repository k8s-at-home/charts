{{/* Make sure all variables are set properly */}}
{{- include "common.values.setup" . }}

{{/* Append the configMap to the additionalVolumes */}}
{{- define "wallabag.configmap.volume" -}}
name: wallabag-parameters
configMap:
  name: {{ template "common.names.fullname" . }}-parameters
{{- end -}}

{{- $volume := include "wallabag.configmap.volume" . | fromYaml -}}
{{- if $volume -}}
    {{- $additionalVolumes := append .Values.additionalVolumes $volume }}
    {{- $_ := set .Values "additionalVolumes" (deepCopy $additionalVolumes) -}}
{{- end -}}

{{/* Append the configMap volume to the additionalVolumeMounts */}}
{{- define "wallabag.configmap.volumeMount" -}}
name: wallabag-parameters
mountPath: /var/www/wallabag/app/config/parameters.yml
subPath: parameters.yml
{{- end -}}

{{- $volumeMount := include "wallabag.configmap.volumeMount" . | fromYaml -}}
{{- if $volumeMount -}}
    {{- $additionalVolumeMounts := append .Values.additionalVolumeMounts $volumeMount }}
    {{- $_ := set .Values "additionalVolumeMounts" (deepCopy $additionalVolumeMounts) -}}
{{- end -}}

{{/* Render the templates */}}
{{ include "common.all" . }}
